# SoloDock website

Static source-project page. Open index.html locally or serve this directory over HTTP. No npm dependencies or build step.

Pages deployment is manual and requires configuration on the repository. Creating the repository does not deploy the website. No nonexistent installers are advertised.
